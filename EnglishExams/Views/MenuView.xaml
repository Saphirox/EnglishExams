<UserControl x:Class="EnglishExams.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EnglishExams.Views"
             xmlns:resx="clr-namespace:EnglishExams.Resources"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource DarkBackgroundTheme}">
        <Grid VerticalAlignment="Center">
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0">
                <Button Content="{x:Static resx:CommonResources.PassATest}"></Button>
            </Border>
            
            <Border Grid.Column="1">
                <Button Content="{x:Static resx:CommonResources.ReviewGradebook}"></Button>
            </Border>
            
            <Border Grid.Column="2">
                <Button Content="{x:Static resx:CommonResources.CreateANewTest}"></Button>
            </Border>
        </Grid>
    </Grid>
</UserControl>
